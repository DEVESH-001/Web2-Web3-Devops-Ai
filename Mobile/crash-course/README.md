# EXPO

`Hooks in React Native + Expo`

1. react-native-safe-area-context

- Provides access to safe area insets for iOS and Android devices
- Used to position UI elements within the safe area of the screen
- Requires wrapping the app with `SafeAreaProvider`
